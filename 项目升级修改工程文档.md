下面这份内容直接按“可落地改造执行清单”写，不再做泛泛解释。先给基线判断：这个仓库当前已经把 `train_gfn.py` 作为 GFN 挖掘入口，把 `DatasetMeta / ParquetFeatureLoader / FeatureRegistryManager / build_search_space / AlphaPoolGFN / SequenceEncoder / torchgfn Sampler` 串起来；`run_adaptive_combination.py` 已经承担表达式池读取、因子张量评估、滚动组合和结果输出；`preprocess_features.py` 负责预填充 parquet；`build_digged_factors.py` 负责把表达式池批量回放为因子表。README 目前本身也不是完整说明书，而是一份“后续工程改造方向草案”。因此，本次改造不要再围绕 README 补字，而是直接把项目升级为“数据准备—挖掘—筛选—生命周期—输出归档”五段式工程。([GitHub](https://github.com/789453/Finance_factor_digging "GitHub - 789453/Finance_factor_digging: Finance_factor_digging · GitHub"))

## 一、改造目标和边界

本次改造的边界要锁死：\*\*不改 AlphaSAGE/Alpha-GFN 的核心表达式生成逻辑，不改 torchgfn 的采样/损失主干，不改 GNN 编码思路；只改数据入口、任务组织、参数化、筛选、生命周期和输出管理。\*\*AlphaSAGE 方法本身的核心是结构感知编码、GFlowNet 探索和稠密奖励；仓库里 `SequenceEncoder` 已经提供 `gnn / transformer / lstm` 三种编码器，其中 GNN 分支使用 `RGCNConv` 按 token 图建模，这与 AlphaSAGE 的结构编码方向是一致的。torchgfn 也明确把 environment、estimator、sampler、gflownet loss 设计成可拆分模块，因此现在最正确的做法不是重写核心，而是在外层建立更稳的作业系统和筛选系统。([arXiv](https://arxiv.org/html/2509.25055v1?utm_source=chatgpt.com "Structure-Aware Alpha Mining via GFlowNets for Robust ..."))

## 二、现有代码基线定位

现有代码职责可以直接拆成下面几块：

`train_gfn.py`：主入口，完成随机种子、日期区间解析、`DatasetMeta` 装载、`ParquetFeatureLoader` 训练/测试数据构造、动态特征枚举、搜索空间构造、目标定义、`AlphaPoolGFN` 建立、`SequenceEncoder` + `DiscretePolicyEstimator` + `EntropyTBGFlowNet` 初始化，以及训练循环和 checkpoint 输出。它当前已经暴露 `--task_config --dataset_meta --domain --operator_names --delta_times --constants --max_expr_length` 等参数，但缺少 job 级别的 family/segment/frequency/batch 配置，也没有把 `CacheManager` 真正接入训练主流程。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/train_gfn.py "raw.githubusercontent.com"))

`ParquetFeatureLoader`：是当前最关键的数据入口。它已经支持 `dataset_meta` 覆盖 loader 参数、raw/filled parquet 自动选择、基于 registry 的特征列解析、静态池/动态池过滤、把特征组装成 `(n_days, n_features, n_stocks)` 张量，并保留 `feature_map` 给表达式解析器使用。但它当前仍然以 `domain=A/B/C/E` 为主维度，路径决策仍然是硬编码 raw/filled 文件映射，频率组、数据层级、列块化、分片缓存、日期越界验证都没有做完。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/parquet_feature_loader.py "raw.githubusercontent.com"))

`FeatureRegistryManager`：当前用 `feature_registry.csv` 管理特征，核心字段实际上已经至少隐含了 `domain / feature_name / allow_in_alphaprobe / fill_policy`，并且会动态创建 `FeatureType`，保留 `CLOSE=0` 作为目标基准。这个模块是你做“两层输入设计”的正确挂载点。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/feature_registry_manager.py "raw.githubusercontent.com"))

`alpha_gfn/search_space.py + alpha_gfn/config.py`：当前搜索空间还是“全局统一搜索空间”，操作符列表在 `config.py` 中写死，`build_search_space()` 只支持 CLI 传 operator/delta/constant 覆盖。这个模块需要升级成“按因子族配置搜索空间”，但不需要动表达式环境本身。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alpha_gfn/config.py "raw.githubusercontent.com"))

`AlphaPoolGFN`：当前池管理逻辑是“按单因子 IC 和互相关阈值”做入池/拒绝/替换，另带一套 embedding 近邻一致性 SSL reward 逻辑。但从训练入口看，`CacheManager` 和 `try_new_expr_with_ssl()` 目前都没有被主流程有效使用，说明 SSL/缓存这两块还没有形成真正闭环。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alpha_gfn/alpha_pool.py "raw.githubusercontent.com"))

`run_adaptive_combination.py`：当前负责后处理与组合。它会把表达式池读成表达式对象，评估 IC/RIC/RET，做 rolling/expanding 窗口组合，支持轻量去重和 ridge/lstsq 回归。**但它当前筛选因子的主条件是** **`RIC`** **和** **`RICIR`** **阈值，而不是你明确希望优先使用的** \*\*`ICIR / TopK Return / Stability`。\*\*这里必须改。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/run_adaptive_combination.py "raw.githubusercontent.com"))

`preprocess_features.py` 和 `build_digged_factors.py`：前者负责域内特征预填充，后者负责表达式回放和输出 parquet。前者目前统一做“时序前后填充 + 截面中位数填充 + 全局 0 填充”；后者甚至会在最终因子回放阶段再做一轮较激进的时序/截面填充。这对“部署表”可以接受，但对“研究期评估”不能默认打开，否则会污染因子原始有效性。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/preprocess_features.py "raw.githubusercontent.com"))

## 三、总工程结构：直接改成五段式

建议直接改成下面这套结构，不要再把所有逻辑继续堆在 `train_gfn.py` 和 `run_adaptive_combination.py` 上：

- `src/data_prep/`
  - `prepare_dataset.py`
  - `build_atomic_features.py`
  - `build_sample_pool.py`
- `src/mining/`
  - `mine_factors.py`
  - `family_search_space.py`
  - `job_spec.py`
- `src/screening/`
  - `screen_factors.py`
  - `metrics_engine.py`
  - `stability_engine.py`
- `src/lifecycle/`
  - `factor_registry.py`
  - `refresh_factor_store.py`
- `src/common/`
  - `manifest.py`
  - `logger.py`
  - `progress.py`

旧文件不要立刻删，而是做迁移：

- `train_gfn.py` 作为兼容入口，只负责把 CLI 转成 `MiningJobSpec` 后调用 `mining/mine_factors.py`
- `run_adaptive_combination.py` 只保留“组合评估”职责，不再承担主筛选职责
- `preprocess_features.py` 保留为原始预处理兼容脚本
- `build_digged_factors.py` 保留为表达式回放脚本，但只用于产出最终物化因子和摘要，不参与筛选决策主链路。([GitHub](https://github.com/789453/Finance_factor_digging/tree/main/src "Finance_factor_digging/src at main · 789453/Finance_factor_digging · GitHub"))

## 四、输入层设计：只保留两层输入

你已经明确，不要模板层。这里直接定稿为两层：

### 4.1 Raw Layer

只放原始字段，来自源 parquet。字段必须带元数据：

- `feature_name`
- `domain`
- `freq_group`
- `fill_policy`
- `source_file`
- `dtype`
- `status`

### 4.2 Atomic Layer

只放手工或规则构建的原子派生字段，不放表达式模板块。原子派生必须短、稳、可解释，且一律物化存储，不在训练时临时生成。推荐最小原子集合：

- 价格动量：`ret_1 / ret_5 / ret_10 / ret_20`
- 波动：`vol_5 / vol_10 / vol_20 / hl_spread / oc_gap`
- 量价偏离：`vwap_dev / amt_z20 / turnover_z20 / volume_cv_20`
- 排名型：`ret_rank_20 / turnover_rank_20 / amount_rank_20`
- 时序关系：`corr_close_volume_20 / corr_ret_amount_20`
- 稳定性：`skew_ret_20 / kurt_ret_20 / downside_vol_20`
- 结构中性后字段：`industry_neutral_ret_5 / beta_20 / residual_ret_5`

接入方式不要放到 `alpha_gfn/config.py`；放到 registry 和独立 builder。也就是：

1. 新增 `data/registry/feature_registry_raw.csv`
2. 新增 `data/registry/feature_registry_atomic.csv`
3. 新增 `src/data_prep/build_atomic_features.py`
4. `FeatureRegistryManager` 统一读取 registry，并增加 `layer` 字段，只在 job 里决定是否开放 raw / atomic。当前 `FeatureRegistryManager` 已经是动态特征入口，最适合直接扩展，不要另起一套特征系统。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/feature_registry_manager.py "raw.githubusercontent.com"))

## 五、DatasetMeta 要升级，但保持兼容

现有 `DatasetMeta` 已经能承载 `name / frequency / domain / data_dir / files / columns / date_range / sample_pool / supported_operators / recommended_delta_times / recommended_constants`，并能吐出 `to_loader_kwargs()` 和 `get_search_space_hints()`；但它现在把 `domain` 限死在 `A/B/C/E`，`frequency` 限死在 `daily/30min/5min`，这对你后面做多域、多区域、多类型、多频率并不够。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/dataset_meta.py "raw.githubusercontent.com"))

建议新增字段，但保留旧字段兼容：

```
{
  "dataset_id": "cn.a_share.equity.daily.v1",
  "name": "cn_a_share_daily",
  "domain": "A",
  "region": "cn",
  "market": "a_share",
  "asset_type": "equity",
  "frequency": "daily",
  "freq_group": "eod",
  "timezone": "Asia/Shanghai",
  "calendar": "SSE",
  "data_dir": "data/factor_ready",
  "files": {
    "raw": "feature_A_price_volume.parquet",
    "filled": "feature_A_filled.parquet",
    "atomic": "feature_A_atomic.parquet"
  },
  "columns": {
    "date": "trade_date",
    "code": "ts_code",
    "close": "close"
  },
  "date_range": {"start": "20100101", "end": "20251231"},
  "sample_pool": "data/basic/feature_ready/sample_pool_200.json",
  "operator_whitelist": ["Ref","TsMean","TsStd","TsRank","TsDelta","TsCorr","TsCov","Add","Sub","Mul","Div","Rank"],
  "recommended_delta_times": [5,10,20,40,60],
  "recommended_constants": [0.5,1.0,2.0],
  "layers_enabled": ["raw","atomic"]
}

```

修改要求：

- `DatasetMeta.validate()`：去掉对 `domain in [A,B,C,E]` 和 `frequency in [daily,30min,5min]` 的硬编码；改成枚举可配置。
- `DatasetMeta.to_loader_kwargs()`：新增 `atomic_file`、`freq_group`、`dataset_id`、`layers_enabled` 输出。
- `DatasetMeta.resolve_parquet_paths()`：要同时返回 raw/filled/atomic。
- 新增 `validate_request(start_time,end_time,freq_group,layer)`，由 loader 在初始化时强制校验。\
  这块 README 已经提出继续完善 meta 和 loader 的方向，当前代码也已经接入了 `--dataset_meta` 参数，所以这是顺着现有结构升级，不是新开分支。([GitHub](https://github.com/789453/Finance_factor_digging "GitHub - 789453/Finance_factor_digging: Finance_factor_digging · GitHub"))

## 六、ParquetFeatureLoader 必须重构成真正的数据视图构造器

当前 `ParquetFeatureLoader` 的问题不是不能用，而是耦合过高。它现在同时做路径选择、日期扩展、daily close 合并、静态池/动态池过滤、特征列回退解析、张量组装和 `feature_map` 构造。你现在要做的是“保留接口，拆内部职责”。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/parquet_feature_loader.py "raw.githubusercontent.com"))

建议改造为四步：

### 6.1 保留 `ParquetFeatureLoader` 名字，对外接口不变

新增参数：

- `dataset_id`
- `freq_group`
- `layers`
- `feature_names`
- `segment_name`
- `read_mode`
- `atomic_path`
- `return_close_only`

### 6.2 内部拆出 4 个私有函数

- `_resolve_input_files()`
- `_build_date_stock_index()`
- `_load_feature_block(file, feature_list)`
- `_assemble_tensor(blocks)`

### 6.3 路径解析逻辑调整

当前 `_resolve_parquet_path()` 只是 raw 和 filled 二选一。改成：

1. 先看 `dataset_meta.files`
2. 再看 CLI 覆盖
3. 支持 raw / filled / atomic 同时存在
4. 支持 `read_mode=raw|filled|atomic|mixed`

### 6.4 缓存视图

对于每个 `(dataset_id, freq_group, start, end, sample_pool_hash, layer_set, status_filter)` 生成一次 view cache：

- `tensor.pt`
- `dates.npy`
- `stocks.npy`
- `feature_index.json`
- `view_manifest.json`

训练和筛选都先读这个 cache，不直接反复扫 parquet。现在仓库里 `CacheManager` 已经有表达式值缓存和 reward 缓存，但 data view cache 还没有落。这个 data view cache 要优先做，因为它对吞吐的提升远大于 reward cache。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alpha_gfn/cache_manager.py "raw.githubusercontent.com"))

## 七、预处理和原子特征构建：分别落两个脚本

### 7.1 `preprocess_features.py`

当前这份脚本的流程是：

- 全表读入
- `ts_code` 内 ffill/bfill
- `trade_date` 内 median fill
- 全局 0.0 fill
- 输出 `feature_{domain}_filled.parquet`。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/preprocess_features.py "raw.githubusercontent.com"))

直接改：

- 增加 CLI：`--domains --input_dir --output_dir --chunk_rows --ffill_limit --bfill_limit --cross_section_fill --global_fallback --log_file`
- 支持按列块处理，不要一次把整表所有 feature 列读进内存
- 每个 domain 输出：
  - `feature_{domain}_filled.parquet`
  - `fill_stats_{domain}.json`
  - `fill_report_{domain}.csv`
- 日志改成 `logging` + `tqdm`
- 把当前硬编码的域文件映射移动到 `config/datasets/*.json`

### 7.2 新增 `build_atomic_features.py`

输入：raw/filled parquet + registry\
输出：atomic parquet + atomic registry augment\
要求：

- 每个 atomic feature 都要有 `formula_name / source_cols / params / fill_policy / freq_group / status`
- 构建完成后写：
  - `feature_{domain}_atomic.parquet`
  - `atomic_feature_stats_{domain}.json`
  - `atomic_registry_{domain}.csv`

执行顺序固定为：\
`raw -> filled -> atomic -> loader view cache`

## 八、搜索空间：从“全局参数”改成“因子族参数”

当前 `build_search_space()` 只支持从 CLI 传 `operator_names / delta_times / constants`，而默认集合来自 `alpha_gfn/config.py`。你现在需要的是一个**因子族配置文件**，不是继续加 CLI 参数。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alpha_gfn/config.py "raw.githubusercontent.com"))

建议新增 `config/factor_families/*.yaml`，每个 family 一个文件，例如：

```
family_id: pv_ts_core
enabled_layers: [raw, atomic]
allowed_domains: [A]
allowed_freq_groups: [eod]
operator_whitelist:
  - Ref
  - TsMean
  - TsStd
  - TsRank
  - TsDelta
  - TsCorr
  - Add
  - Sub
  - Mul
  - Div
  - Rank
delta_times: [5, 10, 20, 40, 60]
constants: [0.5, 1.0, 2.0]
max_expr_length: 18
min_ts_operator_count: 1
forbid_operators: [Greater, Less, Pow]
feature_scopes:
  raw: [open, high, low, close, vwap, amount, volume, turnover]
  atomic: [ret_5, ret_10, vwap_dev, amount_z20, corr_close_volume_20]
sample_budget: 20000
pool_capacity: 200

```

改造点：

- `build_search_space()` 新增 `family_spec` 参数
- `train_gfn.py` 不再直接从 CLI 读 `operator_names / delta_times / constants`，而是优先读 family，再允许 CLI 少量覆盖
- 语法约束里强制“至少 1 个时序算子”
- 你不想要模板层，所以不要在 search space 中加入模板块 token。

这一步改完后，才算真正满足“每个因子的组合参数独立”。

## 九、`train_gfn.py` 改造清单

这是最关键的执行清单。

### 9.1 拆函数

把当前 `train()` 拆成下面几个函数：

- `build_job_context(args)`
- `build_train_test_loaders(job_ctx)`
- `build_target_expression(job_ctx)`
- `build_family_search_space(job_ctx)`
- `build_gfn_components(job_ctx)`
- `run_training_loop(job_ctx, components)`

### 9.2 增加新参数

新增：

- `--job_spec`
- `--family_id`
- `--segment_name`
- `--freq_group`
- `--layers`
- `--cache_root`
- `--candidate_export_freq`
- `--final_candidate_path`

保留旧参数兼容，但 job\_spec 优先。

### 9.3 真正接入 `CacheManager`

当前 `CacheManager` 被 import 但没有真正用起来。必须在 `train_gfn.py` 里：

1. 按 `(dataset_id, date_range, family_id, operator set, constants, windows, max_expr_length)` 生成 `config_hash`
2. 构造 `CacheManager`
3. 注入 `AlphaPoolGFN`
4. 在表达式评估路径中优先查 expr cache 和 reward cache

### 9.4 候选池输出改成持续写

当前只有 checkpoint 保存 `pool_{episode}.json`。改成：

- `candidate_pool/pool_snapshot_{episode}.json`
- `candidate_pool/best_exprs_latest.json`
- `candidate_pool/candidate_metrics_latest.parquet`

每次 `log_freq` 都输出，不只保存模型。

### 9.5 日志和进度

当前只有 `tqdm` 和 tensorboard。加上：

- `logging` 到 `train.log`
- `metrics.jsonl`
- `stderr.log`
- `resource_usage.jsonl`

`GFNLogger.log_metrics()` 增加：

- best\_icir\_train
- pool\_unique\_count
- cache\_hit\_rate
- invalid\_expr\_count
- expr\_accept\_rate

### 9.6 训练主循环不要直接面向最终结果

训练输出只定义为“候选表达式池”，不负责最终准入。\
也就是：**挖掘脚本只挖，不做最终筛选。**

## 十、`AlphaPoolGFN` 改造清单

当前 `AlphaPoolGFN` 做了三件事：

- 计算单因子 IC 和互相关
- 相关性阈值控制入池
- embedding 近邻一致性 SSL reward。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alpha_gfn/alpha_pool.py "raw.githubusercontent.com"))

结合你的新要求，改法如下：

### 10.1 入池闸门变轻

你已经明确冗余和组合贡献不是第一优先，因此这里不要用太严苛的相关性闸门。\
建议：

- `ic_mut_threshold` 默认从 `0.3` 提到 `0.95`
- 只做极端重复剔除，不做强相关大规模否决
- 完整冗余处理放到离线筛选阶段

### 10.2 在线 reward 保留信息性主导

在线 reward 建议只保留：

- `abs(ic_ret)`
- `basic novelty`
- `optional ssl reward`

其中 novelty 不再作为强硬过滤，只作为辅助分数。

### 10.3 加入轻量 TopK 预估

在 `try_new_expr()` 里加一个可选快速指标：

- `topk_ret_fast`
- `ic_sign_consistency_fast`

这两个指标只用于在线排序，不用于终判。

### 10.4 `try_new_expr_with_ssl()` 若未在 env 主链路使用，就不要保留“半接入状态”

二选一：

- 要么接入环境 reward
- 要么把 SSL 逻辑降为离线分析模块

不要继续保持“代码存在但训练不用”的状态。

## 十一、筛选系统：单独新增 `screen_factors.py`

这里是你最关心的部分。结论直接给：**不要再把主筛选放在** **`run_adaptive_combination.py`** **里。** 当前它主要按 `RIC` / `RICIR` 阈值挑因子，这和你的目标不一致。应新建独立筛选脚本。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/run_adaptive_combination.py "raw.githubusercontent.com"))

### 11.1 输入

- 一个或多个 `pool_snapshot_*.json`
- 或多个 `candidate_metrics_latest.parquet`
- 一个 `screening_job.yaml`

### 11.2 输出

- `screen_report.parquet`
- `screen_report.csv`
- `selected_factors.json`
- `factor_cards/*.json`

### 11.3 核心指标只保留三大类

主指标：

1. `ICIR`
2. `TopK Return`
3. `Stability`

辅指标：

- `IC`
- `RIC/RICIR`
- `coverage`
- `turnover_proxy`
- `corr_with_existing_pool`

### 11.4 Stability 定义固定化

建议直接固化为 4 个字段：

- `stability_ic_positive_ratio`：IC>0 天数占比
- `stability_monthly_icir`：月度 ICIR 中位数
- `stability_rolling_icir_p25`：滚动窗口 ICIR 的 25% 分位
- `stability_yearly_sign_consistency`：分年 IC 均值同号占比

### 11.5 TopK Return 参数化

当前 `adaptive_metrics.py` 里 TopK 是固定 top 10%。改成：

- `topk_list = [0.05, 0.10, 0.20]`
- 默认主指标取 `topk=0.10`
- 额外输出 `topk_spread_5_20` 看极值依赖

### 11.6 总评分

你要求“主要看信息性和 ICIR 稳定性”，因此总分建议：

`score = 0.45 * z(ICIR) + 0.30 * z(TopKReturn_10) + 0.25 * z(StabilityScore)`

RIC 只作为辅助列，不进主分或只占 0.05 次级修正。

### 11.7 冗余处理只做轻筛

你不想设太严闸门，那就只保留三条：

- 完全相同表达式：去重
- 因子值相关性 > 0.98：同簇只保留最高分
- 与 active pool 相关性 > 0.995：仅标记 `near_duplicate`

其余不拦截，只打标签。

## 十二、`run_adaptive_combination.py` 的改造要求

这个文件不要删除，但要“降级”为组合评估器，不再承担核心筛选器职责。

### 必改项 1：主筛选条件改掉

当前是：

- 先算 `ic_mean / ic_std / ric_mean / ric_std`
- 然后按 `threshold_ric` 与 `threshold_ricir` 选 `good_factors`
- 最后回归组合。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/run_adaptive_combination.py "raw.githubusercontent.com"))

改成：

- 先从 `screen_factors.py` 读已经筛好的 `selected_factors.json`
- 这里只负责组合回测和滚动更新
- 若允许“在线再筛选”，则只按 `ICIR / TopK / Stability` 的列做排序，不再用 RIC 当主阈值

### 必改项 2：`load_alpha_pool_by_path()` 禁用 `eval`

当前 fallback 使用 `eval(...)`，这对工程稳定性和可复现性都不好。保留 `ExpressionParser(feature_map)` 即可，不要再允许 `eval` 分支。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/run_adaptive_combination.py "raw.githubusercontent.com"))

### 必改项 3：缓存键升级

当前 `metrics_cache_{pool_name}.pt` 只按 pool 名命名，容易跨数据集、跨区段碰撞。改成：

- `metrics_cache_{dataset_id}_{segment}_{label_days}_{pool_hash}.pt`

### 必改项 4：组合去重默认轻量

`adaptive_runtime.py` 当前提供 `none / cols / full` 三种 dedup。按你的要求，默认保持 `none`，只在明确指定时启用 `cols`。不要把 `full` 设为默认。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/adaptive_runtime.py "raw.githubusercontent.com"))

## 十三、`adaptive_metrics.py` 必改

当前 `get_tensor_metrics_safe()` 已经在算：

- IC
- ICIR
- RIC
- RICIR
- `ret`（按 top 10% 多头均值）。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/alphagen_generic/adaptive_metrics.py "raw.githubusercontent.com"))

你要的改造如下：

### 新增函数

- `compute_topk_return_series(x, y_ret, topk=0.1)`
- `compute_stability_metrics(ic_series)`
- `compute_factor_screen_score(metrics_dict, weights)`

### 修改输出

从当前：

- `ic`
- `icir`
- `ric`
- `ricir`
- `ret`

改为：

- `ic`
- `icir`
- `topk_ret_5`
- `topk_ret_10`
- `topk_ret_20`
- `stability_ic_positive_ratio`
- `stability_rolling_icir_p25`
- `stability_monthly_icir`
- `stability_yearly_sign_consistency`
- `screen_score`

### 保留但降级

- `ric`
- `ricir`
- `ret_sharpe`
- `ret_mdd`

它们继续输出，但不再是第一筛选指标。

## 十四、生命周期管理：新增因子注册表

新增 `src/lifecycle/factor_registry.py`，建立统一注册表 `factor_registry.parquet`。字段固定：

- `factor_id`
- `expr`
- `family_id`
- `dataset_id`
- `freq_group`
- `train_segment`
- `valid_segment`
- `oos_segment`
- `ic`
- `icir`
- `topk_ret_10`
- `stability_score`
- `status`
- `created_at`
- `activated_at`
- `retired_at`
- `replaced_by`
- `source_run_id`

状态只保留：

- `candidate`
- `validated`
- `active`
- `watch`
- `retired`

更新规则：

1. 每个年份区段单独挖掘
2. 每个年份区段单独筛选
3. 只有通过 OOS 的才能进 `active`
4. 后续年份只做：
   - old active 复检
   - new validated 挑战
   - 好的替换差的
5. 差的先去 `watch`，不是立刻删除

## 十五、输出目录标准

统一成下面这套，不要再散落：

```
runs/
  mining/{run_id}/
    manifest.json
    resolved_args.json
    dataset_snapshot.json
    search_space.json
    train.log
    metrics.jsonl
    checkpoints/
    candidate_pool/
    diagnostics/
  screening/{run_id}/
    manifest.json
    input_pools.json
    screen_report.parquet
    selected_factors.json
    factor_cards/
  combination/{run_id}/
    manifest.json
    metrics.csv
    ret_s.npy
    cumulative_returns.png
  lifecycle/{run_id}/
    manifest.json
    factor_registry_before.parquet
    factor_registry_after.parquet
    replacement_log.parquet

```

README 已经明确提出 `manifest.json`、标准输出、task runner 和 batch matrix 的方向；你现在直接落到代码，不要停留在 README。([GitHub](https://github.com/789453/Finance_factor_digging "GitHub - 789453/Finance_factor_digging: Finance_factor_digging · GitHub"))

## 十六、日志和进度条要求

### 日志

统一 `logging`，至少 3 个 handler：

- console
- file
- jsonl metrics

每个脚本都写：

- `run.log`
- `errors.log`
- `metrics.jsonl`

### 进度条

保留 `tqdm`，但要加阶段描述：

- `Loading raw data`
- `Building atomic features`
- `Evaluating candidate factors`
- `Screening OOS windows`
- `Refreshing lifecycle registry`

### 资源指标

每隔固定步数写：

- GPU memory
- CPU rss
- cache hit rate
- expr eval count
- invalid expr count

## 十七、逐步运行和测试

按下面顺序做，不要一次性全改：

### Phase 1：数据底座

- 改 `DatasetMeta`
- 改 `FeatureRegistryManager`
- 改 `ParquetFeatureLoader`
- 加 `build_atomic_features.py`
- 做 loader smoke test

### Phase 2：挖掘任务化

- 给 `train_gfn.py` 加 `job_spec`
- 接入 `family_id / segment_name / freq_group / layers`
- 接入 `CacheManager`
- 候选池持续输出

### Phase 3：筛选独立化

- 新建 `screen_factors.py`
- 改 `adaptive_metrics.py`
- 把 ICIR/TopK/Stability 主筛选逻辑迁过去

### Phase 4：组合器收口

- `run_adaptive_combination.py` 只吃已筛好的因子列表
- 只做组合/滚动权重/最终收益评估

### Phase 5：生命周期

- 新建 `factor_registry.py`
- 新建 `refresh_factor_store.py`
- 做 yearly batch refresh

## 十八、当前仓库里最该立刻修的 12 个点

1. `train_gfn.py` 已 import `CacheManager`，但未实例化和接入。
2. `AlphaPoolGFN` 的 SSL 分支存在，但主训练链未闭环。
3. `run_adaptive_combination.py` 目前以 `RIC/RICIR` 为主筛条件，不符合你的主指标要求。
4. `load_alpha_pool_by_path()` 还保留 `eval` fallback，应删除。
5. `metrics_cache_{pool_name}.pt` 命名过弱，容易冲突。
6. `preprocess_features.py` 的填充规则是全局固定硬编码，应参数化。
7. `build_digged_factors.py` 默认对最终因子再做激进填充，应改成可配置且默认 research 模式关闭。
8. `DatasetMeta.validate()` 仍把 domain 和 frequency 写死。
9. `ParquetFeatureLoader._resolve_parquet_path()` 仍是 raw/filled 二选一，不支持 atomic 层。
10. 搜索空间仍是全局 operator/delta/constant，不是因子族。
11. 输出目录还没有真正的 manifest 和标准化中间结果。
12. 还没有独立的 screening/lifecycle 模块。\
    这些点都可以从当前仓库的入口、loader、cache、pool、adaptive 代码和 README 工程草案中直接定位出来。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/train_gfn.py "raw.githubusercontent.com"))

## 十九、最终执行建议

最终执行上，不要再把目标定成“把 README 写完整”，而要定成下面三个里程碑：

**里程碑 1：** 两层输入系统跑通，能按 `dataset_meta + family_spec + segment` 装载 raw/atomic 并训练。\
**里程碑 2：** 独立筛选系统跑通，主指标只看 `ICIR + TopK Return + Stability`，冗余只做轻筛。\
**里程碑 3：** 生命周期系统跑通，支持按年挖掘、按年筛选、样本外准入、后续替换。

做完这三步，这个项目就从“单次实验脚本集合”升级成“可批量运行、可逐步测试、可追溯输出、可持续更新”的真正工程系统。以上方案是完全顺着当前仓库已有结构落下来的，不需要推翻 AlphaSAGE/GFN 核心，只需要把外围工程补成闭环。([GitHub](https://raw.githubusercontent.com/789453/Finance_factor_digging/refs/heads/main/src/train_gfn.py "raw.githubusercontent.com"))

