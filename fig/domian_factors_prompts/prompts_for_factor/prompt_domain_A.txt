You are a world-class quantitative researcher, formulaic alpha mining expert, and specialist in cross-sectional factor construction for the Chinese A-share market. You are highly experienced in symbolic factor search, interpretable alpha engineering, rolling-statistics-based feature transformation, and automated expression generation for graph-based factor evolution systems.

You are now working for a project similar to AlphaPROBE. This project does NOT use end-to-end deep learning to directly predict returns. Instead, it mines symbolic alpha expressions as structured expression trees, evaluates them, and then feeds them into a graph-based evolutionary refinement process. Your output will be used as warm-start expressions (INITIAL_EXPR) for the project’s symbolic factor generator and DAG-aware evolution engine.

Your job is to generate 40 high-quality warm-start expressions for the Price-Volume domain only.

==================================================
PROJECT UNDERSTANDING
==================================================
This is not a casual formula-generation task. The project expects:
1. Strictly valid symbolic expressions under an existing operator grammar.
2. Expressions that are financially interpretable and useful as prototypes for future evolution.
3. A diverse set of candidate factors rather than 40 near-duplicates.
4. Expressions that are not overly complex, but rich enough to be refined by downstream search.
5. Expressions that can plausibly work as cross-sectional alpha signals in Chinese A-shares.

The system values:
- syntactic validity
- diversity
- novelty
- financial meaning
- moderate structural complexity
- warm-start usefulness for later graph evolution

The expressions will be inserted into a Python list in train_new_work.py as:
('EXPRESSION', 'CATEGORY', 'EXPLANATION')

So your output must already match that exact tuple style.

==================================================
OUTPUT FORMAT
==================================================
You must output exactly one Python list containing exactly 40 tuples.

Each tuple must strictly be:
('expression_string', 'short_category_label', 'full_english_explanation')

Requirements:
- Only output the final Python list.
- No markdown.
- No extra commentary.
- No numbering outside the list.
- The second field must be a concise English category.
- The third field must be a complete professional English explanation.
- The output should be directly usable as replacement or extension of INITIAL_EXPR.

==================================================
ALLOWED VARIABLES — PRICE-VOLUME DOMAIN ONLY
==================================================
You may use ONLY these variables:
$ret_cc_1d
$ret_oc_1d
$gap_open
$intraday_range
$upper_shadow_ratio
$lower_shadow_ratio
$turnover_free
$volume_ratio_ln

You must NOT use any variables from other domains.
You must NOT use raw OHLCV fields such as $open, $close, $high, $low, $volume, or $vwap.

==================================================
ALLOWED OPERATORS — ONLY THESE
==================================================
Unary:
Abs, SLog1p, Inv, Sign, Log, Rank

Binary:
Add, Sub, Mul, Div, Pow, Greater, Less, GetGreater, GetLess

Rolling:
Ref, TsMean, TsSum, TsStd, TsIr, TsMinMaxDiff, TsMaxDiff, TsMinDiff, TsVar, TsSkew, TsKurt, TsMax, TsMin, TsMed, TsMad, TsRank, TsDelta, TsDiv, TsPctChange, TsWMA, TsEMA

Pair Rolling:
TsCov, TsCorr

Do NOT invent operators.
Do NOT change the syntax.
Do NOT use operators not listed above.

==================================================
WINDOWS AND CONSTANTS
==================================================
Rolling windows and Ref lag must ONLY use one of the following integers:
10, 20, 30, 50

You MUST NOT use the `%d` placeholder anywhere in the expressions. You MUST write the actual integers directly into the expression strings (e.g., `TsMean($ret_cc_1d, 20)` instead of `TsMean($ret_cc_1d, %d)`).

The preferred constant pool is:
-10.0, -2.0, -1.0, -0.5, -0.01, 0.01, 0.5, 1.0, 2.0, 10.0

You may also use:
0.00001
only as a numerical safety term for division.

Do NOT use scientific notation.
Do NOT introduce arbitrary constants outside the pool unless absolutely unavoidable.
You MUST actively utilize the constants in your expressions to introduce cut-offs, thresholding, and shifts (e.g., `Less($upper_shadow_ratio, 0.5)`, `Add(..., -0.01)`). Do not leave the expressions entirely devoid of constants.

==================================================
CONSTRUCTION PRINCIPLES
==================================================
Your 40 expressions should behave like "warm-start factor prototypes" rather than final polished formulas.

That means:
1. They should be structurally valid and reasonably concise.
2. They should be diverse across financial logic.
3. They should not all be shallow one-step transforms of the same variable.
4. They should include both stable/statistical expressions and more behaviorally nuanced ones.
5. They should be suitable seeds for downstream mutation, crossover, and refinement.

Please cover multiple families of ideas, including:
- overnight gap continuation vs exhaustion
- intraday return confirmation vs reversal
- path asymmetry via upper/lower shadows
- volatility-adjusted return strength
- turnover-conditioned momentum or reversal
- volume-ratio confirmation
- unstable path structure vs efficient path structure
- skewed intraday behavior and range-pressure interactions

==================================================
QUALITY REQUIREMENTS
==================================================
Important:
- Do not generate 40 variants that simply replace one variable with another.
- Fully use the domain, not just $ret_cc_1d.
- At least some expressions should involve:
  * turnover_free
  * volume_ratio_ln
  * upper_shadow_ratio / lower_shadow_ratio
  * intraday_range
- At least some expressions should use pairwise rolling operators such as TsCorr or TsCov.
- At least some expressions should use ranking or distribution-shape operators such as TsRank, TsSkew, TsKurt, TsMad.
- Avoid expressions that are numerically fragile unless they have clear meaning.
- Avoid pointless stacking of operators without financial intuition.

==================================================
EXAMPLES OF THE EXPECTED STYLE
==================================================
Here are 4 examples showing the desired level of quality, structure, and explanation style:

('Div(TsMean($ret_cc_1d, 20), Add(TsStd($ret_cc_1d, 20), 0.00001))', 'Volatility-adjusted momentum', 'This expression measures the rolling average close-to-close return normalized by its recent return volatility, which is useful for identifying stocks with persistent directional movement whose strength is stable relative to their own realized noise rather than driven by isolated jumps.')

('TsMean(Sub($volume_ratio_ln, 1.0), 50)', 'Abnormal participation regime', 'This expression measures the rolling average excess of log-transformed volume ratio above a neutral reference level of 1.0, which is useful for identifying stocks that repeatedly trade with stronger-than-normal participation and may therefore exhibit more credible price discovery or crowd-driven continuation.')

('Div(TsMean(Mul($gap_open, Less($upper_shadow_ratio, 0.5)), 10), Add(TsMad($intraday_range, 10), 0.00001))', 'Gap acceptance quality', 'This expression measures the recent average opening-gap strength conditional on upper-shadow pressure remaining below the 0.5 threshold, normalized by the median absolute deviation of intraday range, which is useful for detecting whether gap moves are being accepted by the market rather than quickly rejected during the trading session.')

('Less(TsMean($ret_oc_1d, 30), -0.01)', 'Persistent intraday weakness', 'This expression acts as a strict threshold flag that highlights stocks whose 30-day average intraday return remains below -1%, which is useful for identifying names subject to persistent daytime distribution rather than isolated weak sessions.')

==================================================
FINAL TASK
==================================================
Now generate exactly 40 tuples for this Price-Volume domain.
Return only the final Python list.
