You are a world-class quantitative researcher, formulaic alpha mining expert, and specialist in Chinese A-share cross-sectional factor construction. You are highly experienced in intraday structure modeling, opening-vs-closing behavior, hourly path compression, trend-efficiency analysis, VWAP-relative price behavior, volume-time distribution, and symbolic alpha engineering for automated factor mining systems.

You are now working for a project similar to AlphaPROBE. This project mines executable symbolic alpha expressions and then evolves them on a graph / DAG structure. Your output will be used as warm-start expressions (INITIAL_EXPR) for a symbolic expression mining engine. Therefore the expressions must be syntactically valid, financially meaningful, and diverse enough to serve as strong starting prototypes for further factor evolution.

Your task is to generate 40 warm-start expressions for the Intraday Summary domain only.

==================================================
PROJECT UNDERSTANDING
==================================================
This is not a generic “invent some intraday formulas” task. You are generating symbolic factor prototypes for a graph-based formula mining engine. The project expects:
1. Strict syntax validity.
2. Expressions that can be directly parsed and inserted into the codebase.
3. Financially meaningful intraday-behavior factors.
4. Diversity across multiple intraday mechanisms.
5. Moderate complexity: not too trivial, not unnecessarily over-engineered.
6. Expressions that can be mutated and refined later by downstream search.

The system values:
- syntactic validity
- diversity
- novelty
- interpretability
- evolvability
- relevance to Chinese A-share cross-sectional prediction

The expressions must match the project’s INITIAL_EXPR tuple style:
('EXPRESSION', 'CATEGORY', 'EXPLANATION')

==================================================
OUTPUT FORMAT
==================================================
You must output exactly one Python list containing exactly 40 tuples.

Each tuple must strictly be:
('expression_string', 'short_category_label', 'full_english_explanation')

Requirements:
- Only output the final Python list.
- No markdown.
- No extra commentary.
- The second field must be a concise English category.
- The third field must be a full professional English explanation.
- The result should be directly usable as replacement or extension of INITIAL_EXPR in train_new_work.py.

==================================================
ALLOWED VARIABLES — INTRADAY SUMMARY DOMAIN ONLY
==================================================
You may use ONLY these variables:
$hour1_ret
$lasthour_ret
$midday_reversal
$intraday_trend_eff
$hourly_volatility
$volume_concentration_pm
$vwap_close_bias_1h
$high_low_time_skew

You must NOT use variables from other domains.
You must NOT use raw OHLCV or price-volume variables.

==================================================
ALLOWED OPERATORS — ONLY THESE
==================================================
Unary:
Abs, SLog1p, Inv, Sign, Log, Rank

Binary:
Add, Sub, Mul, Div, Pow, Greater, Less, GetGreater, GetLess

Rolling:
Ref, TsMean, TsSum, TsStd, TsIr, TsMinMaxDiff, TsMaxDiff, TsMinDiff, TsVar, TsSkew, TsKurt, TsMax, TsMin, TsMed, TsMad, TsRank, TsDelta, TsDiv, TsPctChange, TsWMA, TsEMA

Pair Rolling:
TsCov, TsCorr

Do NOT invent operators.
Do NOT change syntax.
Do NOT introduce unsupported grammar.

==================================================
WINDOWS AND CONSTANTS
==================================================
Rolling windows and Ref lag must ONLY use one of the following integers:
10, 20, 30, 50

You MUST NOT use the `%d` placeholder anywhere in the expressions. You MUST write the actual integers directly into the expression strings.

The preferred constant pool is:
-10.0, -2.0, -1.0, -0.5, -0.01, 0.01, 0.5, 1.0, 2.0, 10.0

You may also use:
0.00001
only as a numerical safety term for division.

Do NOT use scientific notation.
Do NOT introduce arbitrary constants outside the pool unless absolutely unavoidable.
You MUST actively utilize the constants in your expressions to introduce path-quality thresholds, neutral reference levels, and mild behavioral cut-offs. Typical examples include 0.5 for afternoon-volume concentration neutrality and -0.01 or 0.01 for weak negative or positive intraday move thresholds.

==================================================
DOMAIN FINANCIAL SEMANTICS
==================================================
This domain describes compressed intraday structure summarized into daily usable variables, including:
- first-hour return
- last-hour return
- midday reversal intensity
- intraday trend efficiency
- volatility of hourly returns
- afternoon volume concentration
- close relative to intraday VWAP proxy
- timing asymmetry between intraday high and low occurrence

These variables should be treated as structural information about:
- opening-vs-closing path conflict
- late-session confirmation
- noisy reversal vs orderly trend continuation
- volume-time concentration
- closing strength vs fair-value positioning
- intraday timing asymmetry

==================================================
CONSTRUCTION PRINCIPLES
==================================================
Your 40 expressions should collectively cover multiple forms of intraday summary logic, including:
- early strength confirmed by late-session action
- early strength later reversed or absorbed
- efficient intraday trend persistence
- volatile and unstable path structure
- afternoon participation as confirmation or distribution
- VWAP-relative close behavior
- timing skew between intraday high and low
- interaction among reversal, volatility, and volume concentration
- ranking, skewness, kurtosis, and pairwise structure of intraday summary variables

==================================================
QUALITY REQUIREMENTS
==================================================
Important:
- Do not generate near-duplicate expressions that only swap one intraday variable for another.
- Fully use:
  * $hour1_ret
  * $lasthour_ret
  * $midday_reversal
  * $intraday_trend_eff
  * $hourly_volatility
  * $volume_concentration_pm
  * $vwap_close_bias_1h
  * $high_low_time_skew
- At least some expressions should use pairwise rolling operators such as TsCorr or TsCov.
- At least some expressions should use ranking or distribution-shape operators such as TsRank, TsSkew, TsKurt, TsMad.
- Avoid expressions that are numerically fragile unless they have clear behavioral meaning.
- Avoid purely mechanical complexity without intraday micro-behavior intuition.

==================================================
EXAMPLES OF THE EXPECTED STYLE
==================================================
Here are 4 examples showing the expected quality and explanation style:

('Div(TsMean($intraday_trend_eff, 20), Add(TsStd($intraday_trend_eff, 20), 0.00001))', 'Intraday efficiency persistence', 'This expression measures the rolling average intraday trend efficiency normalized by its recent variability, which is useful for identifying stocks whose intraday price path repeatedly behaves in a directional and orderly fashion rather than oscillating noisily within the day.')

('TsCorr($lasthour_ret, $vwap_close_bias_1h, 30)', 'Closing auction confirmation', 'This expression measures the rolling correlation between tail-hour return and the close-to-intraday-VWAP bias, which is useful for detecting whether late-session strength is consistently associated with closing above intraday fair-value reference levels, a pattern often linked to persistent demand rather than random late-day noise.')

('TsMean(Sub($volume_concentration_pm, 0.5), 50)', 'Afternoon participation bias', 'This expression measures the rolling average deviation of afternoon volume concentration from the neutral 0.5 level, which is useful for identifying stocks where trading activity persistently shifts toward the second half of the session, a pattern that may reflect institutional execution or late-session conviction.')

('Div(TsMean(Mul($midday_reversal, Less($hour1_ret, -0.01)), 10), Add(TsMad($hourly_volatility, 10), 0.00001))', 'Weak-open reversal quality', 'This expression measures average midday reversal conditional on first-hour return being weaker than -1%, normalized by the median absolute deviation of hourly volatility, which is useful for detecting stocks whose early weakness is repeatedly repaired in an orderly rather than chaotic intraday manner.')

==================================================
FINAL TASK
==================================================
Now generate exactly 40 tuples for this Intraday Summary domain.
Return only the final Python list.
