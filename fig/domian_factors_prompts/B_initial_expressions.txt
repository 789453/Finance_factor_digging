[
('TsIr($imb_elg_amt, 20)', 'Institutional persistence', 'This expression measures the information ratio of extra-large order imbalance over a 20-day window, which is useful for identifying stocks with persistent and steady institutional accumulation rather than volatile, short-lived spikes.'),
('TsSkew($flow_divergence, 50)', 'Divergence asymmetry', 'This expression measures the skewness of the divergence between large and small orders over a long window, which is useful for detecting stocks where institutional accumulation occurs in concentrated, positive asymmetric bursts against retail flow.'),
('TsCorr($large_buy_share, $net_mf_to_amount, 30)', 'Smart money sponsorship', 'This expression calculates the rolling correlation between the large-order buy participation and net main-fund flow, which is useful for identifying periods where overall capital inflows are structurally supported by major institutional buyers.'),
('TsCov($imb_elg_amt, $imb_sm_amt, 50)', 'Institutional vs retail covariance', 'This expression computes the covariance between extra-large and small order imbalances, which is useful for diagnosing flow polarization and detecting whether retail investors are systematically taking the opposite side of institutional trades.'),
('TsDelta(TsWMA($flow_divergence, 10), 10)', 'Divergence acceleration', 'This expression evaluates the momentum of the weighted moving average of flow divergence, which is useful for capturing accelerating trend shifts in how large orders deviate from small retail trading behavior.'),
('Div(TsMean($net_mf_to_amount, 20), Add(TsMad($net_mf_to_amount, 20), 0.00001))', 'Main-fund accumulation stability', 'This expression measures the average net main-fund flow normalized by its mean absolute deviation, which is useful for highlighting stocks with steady, low-noise capital accumulation.'),
('TsRank($flow_divergence, 50)', 'Divergence exhaustion', 'This expression ranks the current flow divergence against its recent 50-day history, which is useful for identifying overextended periods of institutional-retail disagreement that may be prone to mean reversion.'),
('TsKurt($net_mf_to_amount, 50)', 'Net moneyflow burstiness', 'This expression evaluates the kurtosis of net main-fund flows, which is useful for identifying stocks characterized by extreme, episodic capital shocks rather than continuous, stable order flow.'),
('TsMean(Sub($imb_md_amt, $imb_lg_amt), 30)', 'Mid-tier vs large divergence', 'This expression measures the rolling average difference between medium and large order imbalances, which is useful for detecting structural transitions where mid-sized participants diverge from the largest institutional players.'),
('TsCorr($large_buy_share, $large_sell_share, 50)', 'Large sponsor disagreement', 'This expression assesses the correlation between large buy and large sell participation, which is useful for identifying conflicted flow regimes where institutional participants are aggressively trading against each other.'),
('TsSum(Greater($net_mf_to_amount, 0.01), 20)', 'Continuous main fund support', 'This expression counts the number of days within the last 20 where net main-fund flow exceeded a material positive threshold, which is useful for identifying consistent, daily sponsorship by major capital.'),
('TsEMA(Sub($large_buy_share, 0.5), 20)', 'Buy-side sponsorship momentum', 'This expression computes the exponential moving average of large buy participation relative to a neutral 0.5 baseline, which is useful for tracking the smoothed momentum of large-order dominance on the bid side.'),
('Div($large_buy_share, Add($large_sell_share, 0.00001))', 'Large order participation asymmetry', 'This expression calculates the raw ratio of large buy share to large sell share, which is useful for detecting severe daily imbalances in institutional participation and assessing pure directional dominance.'),
('TsCorr($imb_sm_amt, $net_mf_to_amount, 20)', 'Retail capitulation correlation', 'This expression measures the rolling correlation between small-order imbalance and net main-fund flows, which is useful for confirming whether retail participants are acting as consistent liquidity providers to accumulating institutions.'),
('TsMinMaxDiff($imb_lg_amt, 30)', 'Large flow expansion', 'This expression evaluates the spread between the highest and lowest large-order imbalances over 30 days, which is useful for gauging the maximum scope of institutional flow volatility and regime expansion.'),
('Sub(TsEMA($large_buy_share, 10), TsEMA($large_buy_share, 30))', 'Large buy share acceleration', 'This expression calculates the difference between fast and slow exponential moving averages of large buy participation, which is useful for identifying short-term acceleration in institutional buying pressure.'),
('TsSum(Less($net_mf_to_amount, -0.01), 30)', 'Persistent distribution phase', 'This expression counts the frequency of days with materially negative net main-fund flow, which is useful for flagging stocks undergoing sustained, structural distribution by large holders.'),
('Mul(Sign($imb_elg_amt), Pow(Abs($imb_elg_amt), 2.0))', 'Extreme institutional imbalance', 'This expression amplifies the magnitude of extra-large order imbalance while preserving its direction, which is useful for emphasizing outsized institutional trading shocks in the cross-section.'),
('Sub($imb_sm_amt, TsMean($imb_sm_amt, 20))', 'Small order retail capitulation', 'This expression measures the current small-order imbalance against its 20-day historical baseline, which is useful for detecting sudden spikes in retail panic or impulsive chasing behavior.'),
('TsMed($flow_divergence, 30)', 'Flow divergence median support', 'This expression extracts the median of the divergence between large and small orders, which is useful for establishing the true central tendency of institutional sponsorship while filtering out daily noise spikes.'),
('TsMean(Greater($large_sell_share, 0.5), 20)', 'Large sell side regime frequency', 'This expression calculates the proportion of recent days where large sell orders accounted for the majority of volume, which is useful for confirming entrenched institutional distribution patterns.'),
('Inv(Add(TsVar($large_buy_share, 20), 0.00001))', 'Large buy share consistency', 'This expression takes the inverse of the rolling variance of large-order buy participation, which is useful for identifying stocks with exceptionally stable and predictable institutional sponsorship.'),
('TsCov($imb_sm_amt, $large_sell_share, 30)', 'Retail panic covariance', 'This expression computes the covariance between small-order imbalance and large sell participation, which is useful for detecting aggressive retail buying into heavy institutional distribution.'),
('TsMax($net_mf_to_amount, 10)', 'Main fund maximum burst', 'This expression captures the single highest net main-fund flow over the past two weeks, which is useful for identifying recent, highly aggressive institutional accumulation events that set near-term support.'),
('TsRank($net_mf_to_amount, 10)', 'Net flow rank reversal', 'This expression tracks the cross-temporal rank of the current net moneyflow within a short 10-day window, which is useful for spotting immediate micro-reversals in institutional capital deployment.'),
('TsWMA(Sub($imb_md_amt, 0.01), 10)', 'Medium-order neutral shift', 'This expression calculates a weighted moving average of medium-order imbalance offset by a minor positive threshold, which is useful for tracking subtle shifts in participation from mid-tier market players.'),
('Div(TsMean($imb_elg_amt, 10), Add(Abs(TsMean($imb_lg_amt, 10)), 0.00001))', 'Extra-large vs Large imbalance ratio', 'This expression divides the short-term average of extra-large imbalances by the absolute average of large imbalances, which is useful for detecting when the very largest players are diverging from standard institutional flow.'),
('TsSkew($large_sell_share, 20)', 'Large distribution skew', 'This expression measures the skewness of large-order sell participation, which is useful for identifying asymmetric distribution regimes where institutional selling occurs in sudden, aggressive waves.'),
('TsRank($imb_elg_amt, 30)', 'Extra-large imbalance ranking', 'This expression evaluates the historical rank of the current extra-large order imbalance, which is useful for contextualizing whether today’s institutional activity is exceptionally strong compared to the recent past.'),
('Sub(TsMean($imb_elg_amt, 10), TsMean($imb_elg_amt, 30))', 'Institutional imbalance trend', 'This expression compares the short-term and medium-term moving averages of extra-large order imbalance, which is useful for identifying trend changes and crossover points in absolute institutional trading pressure.'),
('TsCorr($flow_divergence, $imb_elg_amt, 50)', 'Divergence vs institutional flow correlation', 'This expression calculates the correlation between overall flow divergence and extra-large order imbalance, which is useful for verifying if broader retail-institutional divergence is genuinely driven by the absolute largest block trades.'),
('TsDelta(TsWMA($large_sell_share, 10), 10)', 'Sell-side pressure momentum', 'This expression captures the 10-day change in the weighted moving average of large sell share, which is useful for detecting early signs of accelerating institutional liquidation.'),
('TsSum(Greater($flow_divergence, 0.01), 20)', 'Positive divergence frequency', 'This expression counts the number of days where flow divergence was meaningfully positive, which is useful for assessing the reliability and breadth of institutional sponsorship against retail noise.'),
('Div(TsMean(Mul($flow_divergence, Greater($large_buy_share, 0.5)), 10), Add(TsStd($flow_divergence, 10), 0.00001))', 'Sponsored divergence intensity', 'This expression measures average flow divergence conditioned on dominant large-order buying, normalized by divergence volatility, which is useful for isolating high-conviction institutional accumulation events.'),
('TsMin($net_mf_to_amount, 20)', 'Net moneyflow minimum support', 'This expression identifies the worst net main-fund flow over the past 20 days, which is useful for assessing the maximum distribution risk and establishing worst-case capital flight scenarios.'),
('TsCorr(Sub($imb_elg_amt, $imb_sm_amt), $net_mf_to_amount, 30)', 'Flow polarization correlation', 'This expression measures the correlation between the extra-large vs small order spread and overall net flow, which is useful for identifying regimes where directional market flow is heavily dictated by polarized participant behavior.'),
('Div($imb_elg_amt, Add(Abs($net_mf_to_amount), 0.00001))', 'Institutional to total net flow ratio', 'This expression assesses what fraction of the total net main-fund flow is strictly driven by extra-large order imbalances, which is useful for detecting hyper-concentrated institutional activity.'),
('TsKurt($imb_sm_amt, 30)', 'Small order herd burstiness', 'This expression measures the kurtosis of small-order imbalances, which is useful for identifying stocks prone to sudden, erratic bursts of retail herding and panic.'),
('TsPctChange(Add($large_buy_share, 1.0), 10)', 'Large buy participation growth', 'This expression evaluates the 10-day percentage change in large buy share, safely offset by 1.0, which is useful for spotting rapid relative increases in institutional demand intensity.'),
('Sub(TsMax($imb_elg_amt, 20), TsMin($imb_elg_amt, 20))', 'Extra-large flow volatility spread', 'This expression calculates the range of extra-large imbalances over a 20-day window, which is useful for capturing the total structural volatility of elite institutional capital flows.')
]
